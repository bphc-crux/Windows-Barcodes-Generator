<Page
    x:Class="QR_Code.BasicPage2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QR_Code"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="QR-Code Generator" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <ScrollViewer
            x:Name="itemDetail"
            AutomationProperties.AutomationId="ItemDetailScrollViewer"
            Grid.Column="1"
            Grid.RowSpan="2"
            DataContext="{Binding SelectedItem, ElementName=itemListView}"
            HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.VerticalScrollMode="Enabled"
            ScrollViewer.ZoomMode="Disabled">
            <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <StackPanel>
                <Image x:Name="MyImage" Width="180" Height="180" Stretch="UniformToFill" AutomationProperties.Name="{Binding Title}" Source="Assets/MainLogo.png"/>
                <StackPanel x:Name="itemDetailTitlePanel">
                    <TextBlock x:Name="itemTitle"  Text="Save Options" Style="{StaticResource SubheaderTextBlockStyle}"/>
                    <TextBlock x:Name="itemSubtitle"  Text="You can save the QR-Code in any desired sizes." />
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="Message" HorizontalAlignment="Left"  TextWrapping="Wrap" VerticalAlignment="Top" Height="41" Width="533" IsTextSelectionEnabled="True" FontSize="30" Margin="0,37,0,0" Visibility="Collapsed"/>
                    <ProgressBar x:Name="DownloadProgress" HorizontalAlignment="Left" Height="32" Grid.Row="1" VerticalAlignment="Top" Width="533" Visibility="Collapsed"/>


                    <StackPanel Name="Size" Height="421" Width="187" Grid.Row="2" Grid.ColumnSpan="2" Background="#FF094007" HorizontalAlignment="Left" VerticalAlignment="Top">
                        <RadioButton Name="r300" Content="300x300px" IsChecked="True" FontSize="20"/>
                        <RadioButton Name="r200" Content="200x200px" FontSize="20"/>
                        <RadioButton Name="r100" Content="100x100px" FontSize="20"/>
                        <RadioButton Name="r50" Content="50x50px" FontSize="20"/>
                        <RadioButton x:ConnectionId='1' Name="rcustom" Content="Custom"                           FontSize="20"                              />
                        <StackPanel Orientation="Horizontal" >
                            <TextBox Name="Customt" Width="100" FontSize="20" Visibility="Collapsed"/>
                            <TextBlock Name="Customtb" Visibility="Collapsed" Text="px" FontSize="20"/>
                        </StackPanel>
                        <Button x:ConnectionId='2' x:Name="Save" Content="Save" HorizontalAlignment="Left" Grid.Row="1" VerticalAlignment="Top"                    Margin="-3,0" Width="128"/>
                        
                    </StackPanel>

                </StackPanel>
            </StackPanel>
        </Grid>
            </ScrollViewer>
    </Grid>
</Page>

